# Common-base practices audit

- **Repo:** `credit-memo-drafting`
- **Catalog id:** `credit-memo-drafting` (package `credit_memo`, env prefix `CREDIT_MEMO`)
- **Catalogue reference:** [`common-base-practices.md`](https://github.com/portable-genai/.github/blob/main/common-base-practices.md) (checks A1..G7)
- **Authoritative source:** reconciled to the maintainer's cross-repository audit matrix, authoritative on verdicts.
- **Note:** Each check below was re-run against the current tree (greps + file inspection) with
  this repo's names substituted for the reference repo's (`credit_memo` for `cdd_sow_research`,
  `CREDIT_MEMO` for `CDD`), not assumed from the `cdd-sow-research` scorecard.

Applicability: `credit-memo-drafting` ships a UI (`ui/`) and Terraform (`infra/terraform/`), so `[ui]` and
`[infra]` checks apply. The repo does **not** own a web login (identity is an IAP-injected
assertion via `adapters/gcp/iap_identity.py`; there is no `api/auth.py` / OIDC adapter), so the
`[ui]` login-hardening check **C8 is N-A**. **Load-bearing** checks (a FAIL breaks a shared
catalog guarantee) are A1-A6, C1-C5, D1-D3 and E1. **All 15 PASS**: no load-bearing gap or
FAIL remains. Each row carries its own evidence.

| Check | Verdict | Evidence / gap |
|---|---|---|
| **A1** Hexagonal core, stdlib-only domain `[all]` **(load-bearing)** | PASS | `grep -rE "google\|fastapi\|httpx\|pydantic\|boto3\|azure" src/credit_memo/domain/` returns nothing. |
| **A2** Ports are `@runtime_checkable` Protocols, re-exported once `[all]` **(load-bearing)** | PASS | All 12 Protocols carry `@runtime_checkable`; re-exported from `ports/__init__.py`; `test_all_protocols_are_runtime_checkable` asserts it. Ports are grouped by concern (`observability.py` holds 3, `governance.py`/`safety.py` 2 each) rather than one-file-each, but the decorator + single re-export contract holds. |
| **A3** Swappable profiles by one config value `[all]` **(load-bearing)** | PASS | `CREDIT_MEMO_PROFILE = local\|gcp\|platform\|onprem`; per-port `adapters:` map in `config/settings.yaml`; the offline suite (`110 passed`) runs on `pip install -e ".[dev]"` with no GCP SDK. |
| **A4** One adapter constructor `Adapter(settings)` `[all]` **(load-bearing)** | PASS | `tests/contract/test_port_parity.py::test_adapter_constructs_with_single_settings_arg` parametrises `SDK_FREE_PROFILES` (`onprem`, `local`) x every port. |
| **A5** Lazy cloud imports in cloud adapters `[all]` **(load-bearing)** | PASS | `grep -n "^from google\|^import google" src/credit_memo/adapters/gcp/*.py` returns nothing; the offline leg imports every module. |
| **A6** Contract tests enforce the hexagon; port map cannot drift `[all]` **(load-bearing)** | PASS | Both `test_port_parity.py` and `test_behavioral_parity.py` exist; deleting a binding fails `test_every_port_has_onprem_and_local_bindings`. Minor: the drift guard is one-directional (hand-maintained `PORT_PROTOCOLS` -> settings); there is no full set-equality reverse check, so an *unregistered* settings binding with no Protocol would not fail loudly. |
| **A7** Kernel vs vertical split in the domain `[all]` | PASS | The split is physical, not a label: `domain/kernel.py` OWNS the vertical-neutral machinery (citations and provenance, the LLM envelope, guardrail and redaction verdicts, the audit event, the eval report, agent cards, the shared severity scale) and imports nothing from this package, while `domain/models.py` imports it and keeps every name re-exported so existing import sites are unchanged. The direction is proved by execution in `tests/unit/test_kernel_boundary.py`: a fresh interpreter imports the kernel and asserts `credit_memo.domain.models` never enters `sys.modules`. That test was RED against the previous re-export shim. |
| **A8** Consume platform horizontals via thin delegates `[all]` | PASS | `adapters/platform/remote_*.py` are 50-121 lines, marshalling only; each horizontal concern (guardrail, redaction, KB, audit, evaluation, registry) has a `platform` binding. |
| **B1** Consequential math is deterministic, pure, replayable `[agentic]` | PASS | `_grounded.covenant_status` (single auditable comparison) and `peer_comp_service._median`/`_percentile` are pure stdlib and unit-tested (`test_sub_services.py`: 5 covenant-status + peer-comp cases); the LLM extracts terms / drafts prose / classifies severity only, never overrides a status. |
| **B2** Every claim carries a citation; empty retrieval is a hard error `[agentic]` | PASS | `Citation` on `Covenant`/`RiskFlag`/`CreditMemo`; `_grounded.retrieve_passages` + `memo_service` raise `RetrievalEmptyError` on empty retrieval (never ungrounded). |
| **B3** Maker-checker on every consequential output `[agentic]` | PASS | `CreditMemo.requires_human_review: bool = True` default; `CreditReviewPolicy.requires_review()` always True, `escalates()` only raises the bar (BREACH / HIGH / CRITICAL); asserted in `test_serialization_config_policy.py::test_memo_always_requires_review`. |
| **B4** Bank-owned policy numbers in config, defaults = reference `[all]` | PASS | `PolicySettings.covenant_at_risk_band` is exposed under `policy:` and threaded by the composition root into the pure covenant service; an override test proves the deterministic boundary changes. |
| **B5** Open taxonomy: `StrEnum` vocabularies, engines typed on `str` `[all]` | PASS | All eleven vocabularies are `StrEnum` (via the shared `hex-service-kit` commons): members ARE their wire values and `.value` call sites are unchanged. |
| **C1** Identity resolved server-side; client actor/ACL discarded `[all]` **(load-bearing)** | PASS | Request schemas carry no `actor` field (documented "no `actor` field ... server-verified `Principal`"); `api/security.py::get_principal` resolves a verified `Principal` as a per-route dependency and supplies both the audit actor and the entitlement principals. The IAP refusal is now split by whose failure it is, and the split is read at the API. In `adapters/gcp/iap_identity.py` the `CREDIT_MEMO_IAP_AUDIENCE` check already ran before the assertion header was read, so what changed here is the type it raises and the answer that follows: `IapAudienceUnconfiguredError`, not a plain `IdentityError`; the lazy google-auth import moved inside a `try` and raises `IapVerifierUnavailableError`. Both subclass `EndUserAuthUnavailableError` (declared in `ports/identity.py`, carrying `http_status`) and both answer 503, while an absent or blank assertion header stays a plain `IdentityError` and a bare 401 "authentication required" that tells a caller nothing they could forge with. `ctx.header(...)` is now `.strip()`ped, so a header a proxy or a deployment template rendered blank is refused as MISSING instead of reaching the algorithm pin. `api/security.py::get_principal` orders the `EndUserAuthUnavailableError` branch BEFORE the `IdentityError` branch, and the order is load-bearing: it is a subclass, so the reverse silently swallows it and answers the 401 the whole split exists to avoid. `tests/unit/test_iap_refusal_split.py` guards all of it. RED first, offline against the adapter exactly as it shipped: an unset audience gave `IdentityError: CREDIT_MEMO_IAP_AUDIENCE is not configured; cannot verify IAP assertion` whether or not the caller presented an assertion, and `api/security.py` answered 401 "authentication required" to it, sending an operator after a credential no credential would have supplied; a `"   "` header gave `IdentityError: assertion is not a compact JWS or JWE, so it carries no readable algorithm header`, a malformed token reported for an absent one; and with the `[gcp]` extra blocked, `ModuleNotFoundError: No module named 'google.auth.transport'` escaped `resolve` and `get_principal` for a bare 500 per request. The new module does not import against that tree at all; with only the two `Iap*Error` types injected and the old behaviour left standing it scored 8 failed / 3 passed, and reordering the two `except` branches on the fixed tree leaves 10 passed and fails `test_the_api_answers_a_deployment_failure_with_its_own_status_and_message` alone, 401 where 503 is owed. The fourth divergence from the `adapters/gcp/identity.py` family, the `principals` tuple, was investigated and deliberately NOT changed: this adapter grants `user:<subject>` and that family grants nothing, and it is this one that agrees with `hex_service_kit.federation.principal_from_iap_claims`, so aligning it would strip a verified identity of its own principal and widen nothing. The divergence is recorded in the test module rather than closed in the wrong direction. **The claim half is the commons decision too (2026-08-26).** `resolve()` no longer constructs a `Principal` at all: everything after the signature check is one `hex_service_kit.federation.principal_from_iap_claims(claims, _FEDERATION_POLICY, source="gcp-iap", include_subject_principal=True)` call, and the cryptography stays in the adapter because the kit's core is pure stdlib with zero runtime dependencies and verifies nothing. `_FEDERATION_POLICY` is a reviewed literal carrying `tenant_from_hosted_domain=True`: this deployment configures no domain map and IAP already restricts the audience to one organisation, so the `hd` claim IS the tenant id here, and that is stated as an OPT-IN rather than left to a fallback. `include_subject_principal` is stated for the same reason: this adapter family grants the verified subject its own `user:<subject>` principal while the `adapters/gcp/identity.py` family does not and the difference is an authorization decision, not a formatting one. `tests/unit/test_iap_claim_half.py` guards it, and it was RED first, offline, in three stages. (1) Against the adapter exactly as it shipped the module would not even import: `ImportError: cannot import name '_FEDERATION_POLICY'`, which is the state in which a load-bearing tenant can be emptied by a default nobody wrote down. (2) With only the policy object added and the hand-rolled tail left standing it scored 4 failed / 6 passed, and those four ARE the finding: the `acr` row and the mixed-case `hd` row of the comparison disagreed, and both `test_a_claim_set_from_another_issuer_cannot_become_a_principal` and `test_a_verified_assertion_that_names_nobody_is_refused` reported `DID NOT RAISE`, because with the claim-set precheck removed the hand-rolled tail read `claims["email"]` and built a verified identity out of a token issued by somebody else. (3) After adoption, 10 passed. Three deliberate defects on the fixed tree keep the guard honest: flipping `include_subject_principal` scores 6 failed / 4 passed, clearing `tenant_from_hosted_domain` fails the passthrough guard, and passing a fresh `FederationPolicy()` at the call site instead of the reviewed one scores 4 failed / 6 passed, on the resolved tenant rather than on an attribute. **Two behaviour changes are recorded rather than normalised away.** `assurance` now prefers an `acr` claim where the shipped tail hardcoded `"iap"`; nothing in this repository reads `Principal.assurance` for an authorization decision, so the change is audit-visible only, and IAP assertions do not carry `acr` in practice. The tenant is now lower-cased where the shipped tail passed `hd` through with only `.strip()`; `principal.tenant` is a partition key, so a mixed-case hosted domain would change the partition, and Google normalises `hd` to lower case in practice. Both were found by EXECUTING the commons against this adapter over the same claim sets, not by reading it. |
| **C2** Object-level authz derived server-side; tenant isolation by data tags `[all]` **(load-bearing)** | PASS | Both halves hold. *Tenant isolation:* `_acl_ok` is subset/all-of and fail-closed (empty principals see nothing tagged; an untagged passage stays public reference data), the `tenant:<t>` tag dimension is stamped at ingest from the **verified** principal, and `test_api_identity.py::test_cross_tenant_persona_is_denied_borrower_evidence` proves an other-tenant caller gets zero passages for a guessed borrower id. *Per-borrower least privilege:* `domain/entitlements.py` (`may_access_borrower` / `borrower_scope`) makes the `borrower:<id>` grant conditional on the verified principal rather than minting it from the request body, so an authenticated in-tenant caller cannot name any borrower; all three borrower routes answer **403** before any retrieval, and `test_entitlements.py` + `test_unentitled_principal_is_403_on_every_borrower_route` pin it (both verified RED against the pre-fix behaviour). |
| **C3** Redact before everything `[agentic]` **(load-bearing)** | PASS | Redaction is step 1 of `memo_service._build_inner` (before guardrail INPUT and any model/index/audit call); `AuditEvent` stores `redacted_prompt`/`redacted_response` only. |
| **C4** Jurisdiction-driven PII packs keep the gate honest `[agentic]` **(load-bearing)** | PASS | The SG / HK / JP / AU rows, their checksum validators and the RE2-safe forms now come from the shared, versioned **`pii-kit`** package (pinned to an exact SHA in `requirements-dev.lock`), NOT a local copy: the local regex redactor (`national_patterns_for` + `UNIVERSAL_PATTERNS`), the GCP DLP custom info types (`dlp_redaction._custom_info_types`, using `re2_pattern_for`) and the eval gate's leak check all read that ONE source, selected by `pii.jurisdictions` (`config/settings.yaml` / `CREDIT_MEMO_PII_JURISDICTIONS`); an unknown code degrades to universal email/phone rather than raising. The package's rows are case-insensitive for SG NRIC, cover both parenthesised and bare-keyed HK HKID, include the grouped form of JP My Number, and normalise `[\s-]` in the validator. JP My Number and AU TFN keep their checksums, so a filing's bare digit runs are not masked out of the memo (`test_ordinary_filing_figures_survive`; the residual ~1-in-11 coincidental match is pinned, not hidden). B2 has no bank-account row, so there is no row-ordering hazard. Non-home-jurisdiction proof: one golden case **per market** carries that market's own identifier, and the gate scores 1.0 normally and drops to 0.0 with redaction disabled. |
| **C5** Fail-closed defaults everywhere `[all]` **(load-bearing)** | PASS | `main()` binds via `hex_service_kit.resolve_bind_host` (loopback under the no-auth local profile unless `CREDIT_MEMO_ALLOW_INSECURE_DEMO=1`); Makefile `API_HOST ?= 127.0.0.1`; CORS is `cors_allowlist` (explicit `CREDIT_MEMO_CORS_ORIGINS`, never `*`; dev-origin fallback ONLY under local). Proven by `tests/unit/test_netdefaults.py`. Existing fail-closed elements stand (borrower entitlements 403, cross-tenant denial, fail-closed ACL). |
| **C6** Security-header baseline on every surface `[ui]` | PASS | The nonce-based console CSP and hydration proof remain; the API now adds `nosniff`, `no-referrer`, and HSTS only on managed profiles alongside its frame policy. |
| **C7** S2S calls authenticated, https-only outside loopback `[all]` | PASS | `adapters/platform/_s2s.py` sources `hex_service_kit.s2s`; all six platform delegates (`remote_audit`, `remote_guardrail`, `remote_knowledge_base`, `remote_redaction`, `remote_registry`, `remote_evaluation`) validate their base URL at construction (https-only outside loopback) and attach the S2S bearer + optional signed actor (`S2S_TOKEN` / `S2S_SIGNING_KEY`, headers `X-Cm-Actor`/`-Sig`); the eval client's pre-commons TODO on auth is retired. |
| **C8** Web login flow hardening `[ui]` | N-A | The repo owns no login flow: identity is an IAP-injected assertion (`adapters/gcp/iap_identity.py`); there is no `api/auth.py`, no OIDC adapter, and no PKCE/JWKS/session-cookie code to harden. |
| **C9** Tamper-evident audit with honest limits `[all]` | PASS | `LocalAppendOnlyAuditAdapter` wraps the shared `hex_service_kit.audit.HashChainedAuditLog`: SHA-256 chain over canonical JSON, UPDATE/DELETE rejected by triggers, JSONL export/restore with per-line verification, `verify_chain()` exposed, honest-limits docstring. Proven by `tests/unit/test_audit_chain.py`. The managed profile keeps the locked WORM bucket. |
| **C10** No secret values in the repo `[all]` | PASS | `config/settings.yaml` names only `*_env` vars; a literal-secret scan (`AIza…`, `-----BEGIN`, `sk-…`) over `src/` and `config/` is clean. |
| **D1** Locked, reproducible installs everywhere `[all]` **(load-bearing)** | PASS | `requirements-dev.lock` + `requirements-gcp.lock` are committed (uv pip compile); `pyproject.toml` pins `ruff==0.15.18` exactly, so `ruff format --check` cannot drift; CI installs `-r requirements-dev.lock` then `pip install -e . --no-deps`, and the `Dockerfile` builder installs `-r requirements-gcp.lock` then `pip install --no-deps .`. `ui/package-lock.json` covers the npm side. **A lock can be reproducible and still incomplete, found 2026-08-30:** the `gcp` extra omitted `google-cloud-modelarmor`, which `adapters/gcp/model_armor_guardrail.py` imports lazily. Observed failing first: `_get_client()` raised `ImportError` on the `modelarmor_v1` import, so the `gcp` guardrail binding was dead while this gate, the suite and hosted CI all stayed green. The lazy import is what hid it, because the module imports clean and fails only when the guardrail is exercised. It is now declared and locked; the re-compile added one pin and changed no existing one. |
| **D2** Digest-pinned images, SHA-pinned Actions, dependabot, CI audit `[all]` **(load-bearing)** | PASS | Both `Dockerfile` stages pin `python:3.12-slim@sha256:423ed6ab...`; every workflow `uses:` is a 40-char SHA (no tag refs remain); `.github/dependabot.yml` is present; and CI runs a `supply-chain` job with `pip-audit` over both lockfiles as a hard gate. Caveat: `npm audit --audit-level=high` is **advisory** (`\|\| true`) pending a breaking Next.js major bump; `pip-audit` is unaffected and reports no known vulnerabilities. |
| **D3** Whole gate runs offline, zero org secrets `[all]` **(load-bearing)** | PASS | `ci.yaml` (ruff check + ruff format --check + mypy + `pytest -m 'not integration'`) and `eval-gate.yaml` (`python eval/run_eval.py`) both set `CREDIT_MEMO_PROFILE: local` and reference no `secrets.*`. |
| **D4** Non-root, minimal, healthchecked container `[infra]` | PASS | `Dockerfile`: multi-stage (builder + slim runtime), `USER appuser` (uid 10001), `HEALTHCHECK`, `EXPOSE 8093`, `CREDIT_MEMO_PROFILE=gcp` selected in the image; the runtime stage carries only the copied venv, no build toolchain. |
| **D5** Deploy-time residency/sovereignty, parameterised `[infra]` | PASS | Singapore pinning, CMEK, VPC-SC and WORM controls now include project Org Policy for resource locations and service-account keys; CI runs Terraform fmt/init/validate offline. Live enforcement still needs a named apply and evidence. |
| **E1** Offline eval smoke guards merge; `model-quality-gate` owns promotion `[agentic]` **(load-bearing)** | PASS | `eval/run_eval.py` has the `--mode smoke|gate` split via the shared `agent-eval-kit` scaffold; `remote_evaluation.py` re-based on the shared `PromotionGateClient` (registered bundle `doc2-credit-memo` unchanged, pinned by the respx contract test; S2S headers now attached); gate mode refuses to run outside `CREDIT_MEMO_PROFILE=platform|gcp`. `--use-gcp` kept as an alias. |
| **E2** Safety metric with strictest threshold, no false green `[agentic]` | PASS | `pii_safety >= 0.99` remains the strictest threshold, and all three false-green paths are closed. *Detector drift:* the leak check reads the same shared `pii-kit` rows as the runtime redactor, so a leak means the pipeline re-introduced PII, not that two bespoke implementations disagreed. *A faked subject:* the gate runs the production `LocalRegexRedactionAdapter` (pure regex, no SDK), not a `FakeRedaction` that could pass while the real redactor was broken. *A blind pack:* scoring only off the pack the redactor masks with is a closed loop (a narrowed row can neither mask nor detect, scoring a vacuous 1.0 with the raw id in the audit), so the gate now also runs `pii_kit.planted_leak` (a pack-independent literal check of each case's planted identifier) ALONGSIDE `pack_leak`. Proven able to fail: with redaction disabled every PII-bearing case drops 1.0 -> 0.0 and the gate goes RED. The shared jurisdiction list is itself guarded: a golden case whose market is absent from the configured pack **raises** rather than scoring a vacuous 1.0. |
| **E3** Fixtures and golden data obviously fictional `[all]` | PASS | `eval/datasets/golden_cases.jsonl` header says "ALL data is fictional" with "Acme Manufacturing (FICTIONAL)"; DEMO.md carries the live-data sign-off warning and COMPLIANCE.md states the corpus "must not be used for real". |
| **F1** Demo is code, offline, one command, presenter-paced `[all]` | PASS | `make demo` runs `scripts/credit_memo_demo.py` + `render_credit_memo_ui.py` offline; `make demo-server` is the live presenter-controlled server (`credit_memo_demo_server.py`); no cloud or API key. |
| **F2** Demo cannot rot silently `[all]` | PASS | `scripts/demo_selftest.py` runs two executed stages: the in-process session, then the REAL `ThreadingHTTPServer` on an ephemeral port driven end to end over HTTP with `POST /advance`, asserting every figure read back out of the SERVED bytes against what the running app computed. `scripts/render_credit_memo_ui.py` emits stable `data-panel`, `data-metric`, `data-covenant-*`, `data-risk-*`, `data-peer-*` and `data-memo-*` hooks, so the assertions are styling independent. `tests/browser/test_served_demo_ui.py` drives the same served pages through pinned headless Chromium, clicking the presenter's own Next button and reading each figure from the live DOM; `make demo-browser` runs it and Playwright is pinned in the new `[demo]` extra. Both stages were proved RED by planting a stale hard-coded citation count and stripping a panel hook. Scope note, tightened 2026-08-26 so a skip can no longer stand in for a run: the browser stage still skips when NOTHING said a browser was expected, so a day-one offline install (D3) gates without a browser download, and the served stage is unconditional either way. `DEMO_BROWSER_REQUIRED` is the three-state read that says one WAS expected, and then an absent `[demo]` extra or a failed launch FAILS instead of skipping; the fixture also passes `CHROME_PATH` into `chromium.launch`, so a runner shipping its own chromium is driven rather than ignored. RED first, observed on the pre-change file: with a real Chromium named by `CHROME_PATH`, no Playwright download present and `DEMO_BROWSER_REQUIRED=1`, the suite reported `2 skipped` and exit 0, proving both defects at once. On the changed file the same command is `2 passed`; dropping `CHROME_PATH` gives `2 errors` with `DEMO_BROWSER_REQUIRED is set, so a browser was expected here and this suite must not skip. no pinned browser binary available: BrowserType.launch: Executable doesn't exist`; making the `[demo]` extra unimportable gives a collection `ModuleNotFoundError` instead of a skip. Unset still skips (exit 0), and an EMPTY `DEMO_BROWSER_REQUIRED` refuses with `ConfiguredEmptyError`. The hosted GitHub Actions check's browser step now sets the variable too, and executes it for real: GitHub Actions became the fleet's live CI on 2026-09-02, so this evidence runs on every pull request rather than only locally. The earlier hosted CI check that gated pull requests ran in a project the organization deleted on 2026-08-29. The foundation has been rebuilt and this repository's pull-request check is required again. How far that is proved is `org-metadata/docs/deployment-status.md`'s to say, and it is the only place that should say it: a per-repository copy of a status that changes is a copy that goes stale, which this row did within the hour. |
| **F3** Portability claim is executable `[all]` | PASS | `scripts/portability_demo.py` runs fully offline (profile swap, 12-port interface parity, open-format audit read-back, identity portability) and exits non-zero if any claim fails. |
| **G1** Declared doc authority order, kept true `[all]` | PASS | README declares `SPEC > ARCHITECTURE > COMPLIANCE > README`; no stale shipped-feature contradiction was found. |
| **G2** Compliance mapping table + adopter-owned crosswalk `[all]` | PASS | COMPLIANCE maps catalog principles and now includes an explicitly adopter-owned MAS reference crosswalk with applicability, owner and evidence fields. |
| **G3** Documented, mechanised fork path `[all]` | PASS | `docs/ADOPTING.md` gives the kernel/vertical boundary (neutral types vs the `Borrower`/`Covenant`/`RiskFlag`/`CreditMemo`/`PeerComparison` artifacts), the core-vs-adopter-owned file list, and the human-decision checklist; `scripts/rename_fork.py` is the one-pass, dry-run-first rename of the package (`credit_memo`), CLI (`credit-memo`), env prefix (`CREDIT_MEMO_`) and resource ids (`credit-memo-drafting`), verified via `--dry-run` to plan 467 replacements across 77 files and write nothing (git clean). |
| **G4** Retired `[all]` | N-A (retired) | Retired practice. Releases are tracked by git tag and the `pyproject.toml` version. |
| **G5** Role-specific FAQs referencing sibling systems `[all]` | PASS | `docs/faq/` carries `README.md` + five role FAQs (security, portability, features, adoption, compliance), each naming the owning sibling systems (`agent-guardrail-gateway`, `enterprise-knowledge-base`, `agent-registry`, `model-quality-gate` eval, `agent-observability`, `human-review-console` review, `compliance-advisory`, `architecture-validator`, `onprem-dlp`, `operational-resilience-mapping`) and explaining the boundary rather than duplicating them. |
| **G6** Contribution docs cover full extension touch list `[all]` | PASS | `CONTRIBUTING.md` "Adding a port or adapter" lists the full touch list (add Protocol + re-export, gcp/platform/local/onprem adapters, extend `PORT_PROTOCOLS`, add unit tests) and is enforced by the parity test. |
| **G7** Markdown discipline: minimise em-dashes, validate mermaid `[all]` | PASS | Root docs (README, SPEC, ARCHITECTURE, COMPLIANCE, DEMO, CONTRIBUTING) and `docs/*.md` are all at 0 em-dashes. |

**Verdict counts: 39 PASS, 0 PARTIAL, 0 FAIL, 2 N-A.** All 15
load-bearing checks PASS. Named GCP plan/apply, hosted identity and browser evidence remain
external deployment evidence, not a code-practice verdict.

## Gaps carried to systems/

Recommended `Capability gaps` entries for the `credit-memo-drafting` row of
the maintainer's per-system register.

**Load-bearing set: none open.** Every load-bearing check (A1-A6, C1-C5, D1-D3, E1) PASSes.

**Open quality findings: none.** A7 and F2 both PASS with executed evidence (see their rows).
Named deployment inputs and execution evidence remain outside this scorecard and must not be
inferred from the green offline gates. **G4** is a retired practice.
